{
  "providers": ["python"],
  "buildCmd": "mkdir -p logs proyecto/static/uploads",
  "startCmd": ". /opt/venv/bin/activate && python init_railway_db.py && gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 wsgi:app",
  "variables": {
    "PYTHONPATH": "/app"
  }
}